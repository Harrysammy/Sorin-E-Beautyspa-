# Sorin E BeautySpa — Master Project Manifest

This folder is the deployment-ready consolidation created from the newer, complete source workspace:

`C:\Users\USER\Documents\Codex\2026-08-30\build-x20-2`

The older `build-x20` workspace was inspected but not selected because it only contained an earlier HTML/CSS/JavaScript subset. It has no central configuration, SEO files, logo asset, or local media collection.

## Structure

- `index.html` — main website
- `styles.css`, `media.css` — responsive layout and gallery media styling
- `script.js`, `config.js` — interactive functionality and central business/service data
- `assets/images/` — `p1.jpeg`, `p2.jpeg`
- `assets/videos/` — `p3.mp4`, `v1.mp4`, `v2.mp4`, `v3.mp4`
- `assets/logo/` — official logo JPEG and existing SVG mark
- `robots.txt`, `sitemap.xml` — crawler configuration
- `.env.example` — placeholder-only payment environment configuration; no secrets

The original source workspaces remain unchanged.
